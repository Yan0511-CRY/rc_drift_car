# RC漂移车 — 完整接线图与供电拓扑

> 一句话记住：**信号弱电归 STM32，动力强电走 BEC，图传最好独立供电。**

---

## 一、完整接线总图

```
┌────────────────────────────────────────────────────────────────────┐
│                        2S 锂电池 (7.4V)                            │
│                              │                                     │
│            ┌─────────────────┼─────────────────┐                   │
│            │                 │                 │                   │
│            ▼                 ▼                 ▼                   │
│       ┌──────────┐    ┌──────────┐     ┌──────────┐               │
│       │ 有刷电调  │    │  舵机     │     │ 图传供电  │               │
│       │ Brushed  │    │  Servo   │     │ (独立2S)  │               │
│       │  ESC     │    └────┬─────┘     └────┬─────┘               │
│       └────┬─────┘         │               │                      │
│            │        ┌──────┼──────┐        ▼                      │
│            │        │      │      │  ┌──────────┐                │
│            ▼        ▼      ▼      ▼  │ FPV摄像头 │                │
│       ┌────────┐ ┌─────┐┌─────┐┌────┐│ + 5.8G   │                │
│       │有刷电机 │ │舵机 ││接收机││STM32│ 图传TX   │                │
│       │540/550 │ │Servo││ RX ││F411│└──────────┘                │
│       └────────┘ └──┬──┘└──┬──┘└──┬──┘                          │
│         红黑两根线   │ CH1  │ CH3  │                                │
│                     │转向  │油门  │                                  │
│                     ▼      ▼      │                                  │
│                STM32 PA4   PA5    ├── I2C1 ────── MPU6050         │
│                (EXTI4)   (EXTI5)  ├── I2C2 ────── OLED (PB3/B10) │
│                                   ├── USART1 ──── USB-TTL → 电脑  │
│                                   └── SWD ─────── ST-Link         │
└────────────────────────────────────────────────────────────────────┘

注：有刷电调(如好盈1060)自带BEC 5V/6V输出，可直接给舵机、接收机、STM32供电。
    如果想用独立BEC → 需要挑掉电调信号线的红线，断开电调内置BEC。
```

---

## 二、逐根线详解

### 2.1 供电系统（最重要的部分）

```
锂电池 (2S 7.4V)
    │
    └──→ 有刷电调 电源输入端 (T插/XT60)
            │
            ├──→ 电机输出 红(+)黑(-) 两根线 ──→ 540/550 有刷直流电机
            │
            └──→ 电调信号线 (三色排线):
                    ├─ 白/黄(信号) → STM32 PA1 (TIM2_CH2)
                    ├─ 红(+5V)    → 给舵机/接收机/STM32供电 (电调内置BEC)
                    └─ 黑(GND)    → 公共 GND

供电分配（利用电调内置BEC 5V/6V输出）：

  电调BEC 5V ──┬──→ 舵机 红(+)线   (舵机耗电大，2A)
               ├──→ 接收机 VCC     (约30mA)
               └──→ STM32 "5V" pin (约100mA)

  STM32 3.3V → MPU6050 VCC (约4mA，不要从5V取电!)
  STM32 3.3V → OLED VCC  (约20mA, SSD1306 3.3V)
```

> **有刷电调（如好盈1060/1080）通常内置 BEC**，输出 5V~6V / 2A~3A。
> 如果一块电池同时给电调和 BEC 设备供电，接线就非常简洁：
> 电池 → 电调 → (电机 + 舵机 + 接收机 + STM32 一并由电调BEC供电)。

**电流路径总结：**

| 用电设备 | 电压 | 供电来源 | 最大电流 |
|----------|------|----------|----------|
| 有刷电机 (540/550) | 7.4V | 电池 → 有刷电调 | ~15A (堵转时) |
| 舵机 | 5V~6V | 电调内置BEC → 舵机 | ~2A (堵转时) |
| STM32F411 | 5V | 电调内置BEC → 5V Pin | ~100mA |
| RC接收机 | 5V | 电调内置BEC | ~30mA |
| MPU6050 | **3.3V** | STM32 板载 3.3V 输出 | ~4mA |
| OLED (SSD1306) | **3.3V** | STM32 板载 3.3V 输出 | ~20mA |
| FPV 摄像头 + 图传 | 7~24V | **建议独立小电池** | ~500mA |

> **关键原则**：舵机、电调、STM32 的 **GND 必须全部连通**（共地），否则信号电压没有参考点。

---

### 2.2 信号线连接（按 STM32 引脚分组）

#### 舵机 (Servo)

```
舵机三色线:
┌─────────────────────┐
│  白/黄 — 信号 ────────→ STM32 PA0 (TIM2_CH1)    │
│  红   — 电源(+) ──────→ BEC 5V 输出               │
│  黑/棕 — GND    ──────→ 公共 GND                  │
└─────────────────────┘
```

#### 有刷电调 (Brushed ESC) → 电机

```
有刷电调 (如 好盈QuicRun 1060 / 1080):
┌─────────────────────────────────────────────────┐
│  电源端 (红/黑 粗线):                              │
│    红(+) ─────→ 电池正极 (T插 / XT60)             │
│    黑(-) ─────→ 电池负极                          │
│                                                   │
│  电机端 (黄/蓝 或 红/黑 粗线):                      │
│    黄/红 ─────→ 有刷电机 正极(+)                   │
│    蓝/黑 ─────→ 有刷电机 负极(-)                   │
│   (接反了就是倒转，交换即可)                        │
│                                                   │
│  信号线 (三色排线, 类似舵机线):                     │
│    白/黄 ─────→ STM32 PA1 (TIM2_CH2)  信号        │
│    红    ─────→ 舵机/接收机/STM32 5V   (内置BEC输出) │
│    黑/棕 ─────→ 公共 GND                          │
│                                                   │
│  跳线帽设置 (1060):                                │
│    运行模式: 正反转带刹车 (漂移推荐)               │
│    BEC输出: 6V (给舵机更大的力) 或 5V              │
└─────────────────────────────────────────────────┘
```

**有刷 vs 无刷的区别：**

| 对比项 | 有刷 (Brushed) | 无刷 (Brushless) |
|--------|---------------|-----------------|
| 电机线 | **2根** (红/黑) | 3根 (ABC三相) |
| 电调价格 | 便宜 (1060 ~¥70) | 贵 (60A ~¥100+) |
| 电机价格 | 540/550 ~¥30 | 3650 ~¥120 |
| 低速线性 | **好** (适合漂移精细控油) | 一般 |
| 寿命 | 有碳刷磨损 | 长 |
| 电调BEC | 基本都有内置BEC | 部分没有 |
| 1/10漂移车适用性 | **经典方案** | 竞技级 |

> **为什么漂移车常用有刷？** 有刷电机低转速下的油门线性度更好，
> 对漂移这种需要精细控制后轮打滑的操作很重要。
> 答辩时可以说："选型上权衡了成本和控制精度，有刷方案在低转速段的
> 线性特性更适合漂移场景的油门微调。"

#### BEC 供电说明（有刷电调内置）

```
电调信号线的红线 = 电调内置BEC的5V/6V 输出。

方案A（推荐，最简单）:
  电调红线 → 舵机红线 + 接收机VCC + STM32 5V pin
  一跟红线并接所有5V设备，GND也一样并接。

  但注意: STM32 的 5V pin 同时也接到 ST-Link 的 USB 5V?
  → 烧录时拔掉电调电池，STM32由ST-Link USB供电。
  → 运行时拔掉ST-Link，STM32由电调BEC供电。
  → 两者不要同时供电！(USB 5V 和 BEC 5V 并联可能倒灌)

方案B（独立BEC，更稳）:
  如果担心电调BEC在大油门时掉压影响STM32工作 →
  加一个独立BEC模块 (如 Mini 360) 专门给STM32供电。
  此时需要挑掉电调信号线的红线(不接)，避免两个BEC打架。
```

#### RC 接收机 → STM32

**当前方案：HotRC F-06 接收机，独立 PWM 输出**

F-06 每个通道单独输出 PWM 信号，只需 CH1(转向) + CH3(油门)：

```
接收机 (HotRC F-06):
┌─────────────────────────────────────┐
│  CH1 (转向) ────→ STM32 PA4 (EXTI4)  │  1000~2000μs, 50Hz
│  CH2           → 悬空               │
│  CH3 (油门) ────→ STM32 PA5 (EXTI5)  │  1000~2000μs, 50Hz
│  CH4/CH5/CH6   → 悬空               │
│  VCC ──────────→ BEC 5V              │
│  GND ──────────→ 公共 GND            │
└─────────────────────────────────────┘

工作原理:
   PA4/PA5 配置为 GPIO_EXTI 双沿触发 + 内部上拉
   上升沿 → 记录 DWT 时间戳 (1μs 精度)
   下降沿 → 计算脉宽 → 写入 rc_channels[]
```

**备选方案：PPM 接收机 (如 FS-iA6B / R6FG)**

如果换回 PPM 接收机：

```
接收机:
┌─────────────────────┐
│  CH1 (PPM输出) ──────→ STM32 PA6 (TIM3_CH1)  │
│  其余通道不接         │                        │
│  VCC ────────────────→ BEC 5V                 │
│  GND ────────────────→ 公共 GND                │
└─────────────────────┘
```

| 模式 | 接线 | 使用引脚 |
|------|------|----------|
| **PWM 独立** (当前) | CH1→PA4, CH3→PA5 | EXTI4 + EXTI5 |
| PPM | CH1→PA6 | TIM3_CH1 (输入捕获) |

#### MPU6050 (IMU 陀螺仪)

```
MPU6050 模块 (GY-521):
┌─────────────────────┐
│  VCC ────────────────→ STM32 3.3V pin   (3.3V, 不是5V!)  │
│  GND ────────────────→ 公共 GND                           │
│  SCL ────────────────→ STM32 PB6 (I2C1_SCL)               │
│  SDA ────────────────→ STM32 PB7 (I2C1_SDA)               │
│  AD0 ────────────────→ GND (地址 0x68) 或悬空             │
│  INT ────────────────→ 不接 (用轮询读取即可)              │
└─────────────────────┘
```

> **MPU6050 是 3.3V 器件**，接 5V 会烧。GY-521 模块虽然有 LDO 降压，但稳妥直接接 3.3V。

#### OLED 显示屏 (SSD1306, I2C2)

```
SSD1306 OLED (128×64, I2C 地址 0x3C):
┌─────────────────────┐
│  VCC ────────────────→ STM32 3.3V                           │
│  GND ────────────────→ 公共 GND                              │
│  SCL ────────────────→ STM32 PB10 (I2C2_SCL, AF4)           │
│  SDA ────────────────→ STM32 PB3  (I2C2_SDA, AF9)           │
└─────────────────────┘

注意: PB3 出厂默认为 JTDO/SWO 功能,
      CubeMX 中设为 I2C2_SDA 后自动释放 JTAG (只保留 SWD 调试)。
      不影响 ST-Link 烧录和调试。
```

#### USART1 → USB-TTL → 电脑

```
STM32:
┌─────────────────────┐
│  PA9  (TX) ──────────→ USB-TTL 模块 RX                    │
│  PA10 (RX) ──────────→ USB-TTL 模块 TX (当前只用到TX)      │
│  GND        ──────────→ USB-TTL 模块 GND                    │
│                                                             │
│  USB-TTL 模块 ─────────→ 电脑 USB (串口助手/VOFA+ 显示)     │
└─────────────────────┘
```

#### 烧录/调试 (ST-Link / DAP-Link)

```
STM32:
┌─────────────────────┐
│  SWDIO ──────────────→ ST-Link SWDIO                        │
│  SWCLK ──────────────→ ST-Link SWCLK                        │
│  GND   ──────────────→ ST-Link GND                          │
│  3.3V  ──────────────→ (可选) ST-Link 3.3V (仅检测电压)     │
└─────────────────────┘
```

---

## 三、STM32F411 (BlackPill) 引脚速查表

```
                ┌────────────────────┐
                │     STM32F411       │
                │     BlackPill       │
                └────────────────────┘

    3.3V ─┤1                       28├─ GND
     GND ─┤2                       27├─ 5V ← BEC 5V输入
    RESET─┤3                       26├─ PB9
     PB12─┤4                       25├─ PB8
     PB13─┤5                       24├─ PA6 (空闲)
     PB14─┤6                       23├─ PB4
     PB15─┤7                       22├─ PB3 → I2C2_SDA → OLED
      PA8─┤8                       21├─ PA15
      PA9─┤9 → USART1_TX           20├─ PA12
  Servo  ←├PA0  TIM2_CH1           19├─ PA11
有刷ESC ←├PA1  TIM2_CH2           18├─ PB10 → I2C2_SCL → OLED
接收机CH1←├PA4  EXTI4              17├─ PB1
接收机CH3←├PA5  EXTI5              16├─ PB0
  SCL   ↔├PB6  I2C1_SCL → MPU6050  15├─ PA7
  SDA   ↔├PB7  I2C1_SDA → MPU6050  14├─ PB11(无此引脚)
                └────────────────────┘
```

---

## 四、FPV 图传系统（独立子系统）

```
FPV 独立供电 (小型 2S/3S 电池 或 BEC 输出):
    │
    ├──→ 摄像头 电源 (3.3V~5V 或直接电池电压，看型号)
    │       └─ 视频信号线 → 图传TX 视频输入
    │       └─ GND → 公共GND (可以和动力GND共地，也可以隔离)
    │
    └──→ 图传TX (5.8G VTX, 如 Rush Tank / Eachine TX805)
            └─ 天线: 蘑菇天线 (RHCP 圆极化)
            └─ 频率: 5.8GHz 合法频段 (如 5645MHz CH1)

【接收端】
    FPV 眼镜/屏幕 内置接收机 (5.8G VRX)
    DVR 录像模块 → 录制第一视角视频 (答辩演示用)
```

> **供电隔离建议**：图传和摄像头用一块独立的小 3S 电池（如 450mAh），
> 不和动力电池共用。大油门拉压 → 图传横纹干扰 → 看不清画面。

---

## 五、硬件选型清单

| 零件 | 推荐型号 | 价格参考 |
|------|----------|----------|
| **车架** | 樱花 D5 / MST RMX 2.0 / YD-2 | ¥300~800 |
| **有刷电调** | 好盈 QuicRun 1060 (60A) | ¥70 |
| 或 | 好盈 QuicRun 1080 (80A, 可调参数更多) | ¥140 |
| **有刷电机** | Surpass 540 13.5T / 17.5T | ¥35 |
| 或 | 好盈 550 21T (大扭矩) | ¥50 |
| **舵机** | Power HD 1206TG (数码,高速) | ¥60 |
| 或 | SAVOX 1251MG (竞速级) | ¥150 |
| **电池** | 2S 2200mAh 25C (软包锂电) | ¥60 |
| **图传独立电池** | 2S 450mAh | ¥25 |
| **USB-TTL** | CH340G 模块 | ¥5 |
| **ST-Link** | ST-Link V2 克隆版 | ¥15 |
| **LC 滤波器** | (图传供电滤波，电感和电容自焊) | ¥2 |
| BEC 降压 | Mini 360 (仅当需要独立BEC时) | ¥3 |

---

## 六、上电顺序（安全流程）

```
1. 所有接线完成 → 万用表蜂鸣档测电源和GND之间有无短路
2. 只接 ST-Link (USB供电) → 检查 STM32 是否正常 (LED闪烁)
3. 接 BEC 5V → 检查舵机是否到中位、接收机是否对频成功
4. 最后接动力电池 → ESC 自检音 → 轻推油门测试
5. 起飞！
```

> **永远在动力电池上电前**确认：
> - 遥控器已开机
> - 油门摇杆在最低位
> - 有人在旁协助（第一次测试）
